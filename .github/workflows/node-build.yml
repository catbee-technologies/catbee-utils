name: 🚧 Build and Lint

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']

jobs:
  node-build:
    name: 🧱 Node Build & Lint
    uses: catbee-technologies/.github/workflows/node-build.yml@main
